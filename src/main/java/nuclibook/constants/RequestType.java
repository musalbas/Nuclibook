package nuclibook.constants;

/**
 * The enum values represent the type of the request in all the routes.
 *
 * It can either be GET or POST, depending on the accessed link.
 */
public enum RequestType {
	GET,
	POST
}
