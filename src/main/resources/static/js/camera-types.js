var validateEditForm = function (formObject) {
    var error = false;

    // TODO

    return !error;
};

var validateCreateForm = function (formObject) {
    var error = false;

	// TODO

    return !error;
};